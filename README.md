# Lab 01 — The Event Loop Is the Game Loop

Браузерна гра з космічним кораблем, яким можна керувати за допомогою клавіатури.

Гра використовує фіксований крок симуляції 60 разів на секунду та `requestAnimationFrame` для відображення кадрів на Canvas.

## M1 — Ігровий цикл

Реалізовано функцію `createLoop()`, яка використовує:

* `requestAnimationFrame`;
* фіксований крок симуляції `1 / 60`;
* накопичувач часу;
* обмеження `delta time`;
* інтерполяцію за допомогою `alpha`.

HUD показує:

* Steps/s — кількість кроків симуляції за секунду;
* FPS — кількість кадрів за секунду;
* Frame time — час обробки останнього кадру.

## M2 — Клавіатура та корабель

Обробка клавіатури реалізована за допомогою `createInput()`.

Корабель представлений звичайним JavaScript-об'єктом:

`{x, y, vx, vy, angle, thrust}`

Функція `integrate(ship, input, dt)` відповідає за:

* поворот корабля;
* прискорення у напрямку його руху;
* гальмування;
* обмеження максимальної швидкості.

Симуляція не залежить від Canvas.

При виході корабля за межі арени він з'являється з протилежного боку.

## M3 — Інтерполяція та відображення

Реалізовано:

* збереження попереднього та поточного стану корабля;
* інтерполяцію між станами;
* правильну інтерполяцію кута;
* Canvas з урахуванням `devicePixelRatio`;
* зміну розміру Canvas разом із вікном;
* поворот корабля за допомогою `translate()` та `rotate()`;
* полум'я під час прискорення;
* фонову сітку.

## M4 - Експерименти

### M4.1

Для експерименту було додано синхронне блокування головного потоку на 100 мс кожні 60 кадрів.

Під час блокування гра короткочасно пригальмовує, а виконання JavaScript призупиняється, оскільки `while` займає головний потік. Через це event loop не може виконувати інші задачі до завершення блокуючого циклу.

**Результат експерименту:**

* блокування: 100 мс;
* Steps/s: близько 60;
* FPS: зменшувався під час блокування;
* у грі було помітне короткочасне підвисання.

Цей момент якраз збігався з появою в консолі повідомлення `Blocking: 100 ms`.

Після завершення експерименту блокуючий код було видалено.

Експеримент проводився у файлі `blocking.js`.

<img width="1468" height="747" alt="Снимок экрана 2026-09-10 в 10 00 02" src="https://github.com/user-attachments/assets/4de92300-3a54-499f-9606-c772079abf56" />


### M4.2

**setInterval**

У цьому експерименті `requestAnimationFrame` було замінено на:

`setInterval(frame, 16)`

За допомогою тестового скрипта було автоматично заміряно кількість кадрів та FPS у двох фазах: коли вкладка активна (foreground) та коли користувач перемикається на іншу вкладку (background) на 5 секунд.

**Результати вимірювань (з консолі):**

* Foreground results: 600 кадрів за 10.01 с (FPS ≈ 59.97).
* Background results: 300 кадрів за 5.00 с (FPS ≈ 59.95).
* Overall results: Середній час кадру склав 16.67 мс при низькому джиттері (0.53 мс).

У цьому експерименті було видно, що `setInterval` продовжував виконуватися після переходу на іншу вкладку.

За 5 секунд у фоновому режимі було отримано 300 кадрів.

Також `setInterval` не синхронізований безпосередньо з оновленням екрана. Інтервал у 16 мс трохи відрізняється від часу одного кадру на екрані з частотою 60 Гц, який становить приблизно 16,67 мс.

Через це час між кадрами може трохи змінюватися, що видно за показником нестабільності часу кадру.

Після завершення експерименту `setInterval` було замінено назад на `requestAnimationFrame`.

<img width="2751" height="1479" alt="Снимок экрана 2026-09-09 в 14 54 12 — копия" src="https://github.com/user-attachments/assets/9344775c-67ff-4d2b-8ec0-b0524bc1a3e9" />

### M4.3 - Змінний крок

У цьому експерименті було видалено накопичувач часу, а симуляція виконувалася один раз за кожен кадр із використанням фактичного часу між кадрами.

**Результати вимірювань:**

**Змінний крок без обмеження процесора:**

Кінцева позиція: `(2520.57, 0)`

<img width="1029" height="210" alt="Снимок экрана 2026-09-09 в 17 56 59" src="https://github.com/user-attachments/assets/1d22bf2f-4b66-4ddc-9553-b8f034e9bf99" />

**Змінний крок при обмеженні процесора 6×:**

Кінцева позиція: `(2519.97, 0)`

<img width="675" height="186" alt="Снимок экрана 2026-09-09 в 17 58 31" src="https://github.com/user-attachments/assets/c9ab7bd4-e015-4959-bdf6-aa82453c075f" />

Різниця між результатами склала 0.60.

Після цього було відновлено фіксований крок симуляції.

**Фіксований крок без обмеження процесора:**

Кінцева позиція: `(2508.33, 0)`

<img width="721" height="173" alt="Снимок экрана 2026-09-09 в 17 42 37" src="https://github.com/user-attachments/assets/bb0cb28d-4e2e-4123-a851-7b903747f2fa" />

**Фіксований крок при обмеженні процесора 6×:**

Кінцева позиція: `(2508.33, 0)`

<img width="1062" height="194" alt="Снимок экрана 2026-09-09 в 17 46 14" src="https://github.com/user-attachments/assets/f76acc12-b887-4a98-87f8-782a3aae2bf8" />

При фіксованому кроці кінцева позиція в обох випадках була однаковою.

Це показує, що фіксований крок робить симуляцію стабільнішою та забезпечує однаковий результат при різному навантаженні.
